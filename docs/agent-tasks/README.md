# Agent tasks

Технические задания и планы изменений. Создаются командой **`/analyze`**.

Один файл на задачу: `docs/agent-tasks/<краткое-имя>.md`.

Реализация — команда **`/write`** по согласованному ТЗ.

Файлы задач (кроме этого README) не попадают в git — см. `.gitignore`.
